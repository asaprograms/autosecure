# configuration file example. change name to config.py once options have been set
# can be modified to work from a .env file with minor tweaks.

KEYS = {
    "hypixel": "", # input valid hypixel api key within the quotations, required
    "2captcha": "", # input valid and sufficient balance 2captcha api key within the quotations, required
}